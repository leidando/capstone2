databases:
  - name: cswdo_db
    databaseName: cswdo_system
    user: cswdo_admin
    plan: free

services:
  - type: web
    name: cswdo-management-system
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn cswdo_system.wsgi:application
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.9"
      - key: DATABASE_URL
        fromDatabase:
          name: cswdo_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://cswdo-management-system.onrender.com"
      - key: DJANGO_SUPERUSER_USERNAME
        value: "admin"
      - key: DJANGO_SUPERUSER_EMAIL
        value: "admin@example.com"
      - key: DJANGO_SUPERUSER_PASSWORD
        value: "admin123" # Change this later in the Render Dashboard!
